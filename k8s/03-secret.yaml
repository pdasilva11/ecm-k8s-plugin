apiVersion: v1
kind: Secret
metadata:
  name: vault-credentials
  namespace: vault-services
  labels:
    app: vault-credential-service
type: Opaque
stringData:
  # IMPORTANT: Replace these values with your actual Vault credentials
  vault-username: "vault-user"
  vault-password: "change-me-to-your-vault-password"
